import React from 'react'
import ReactDOM from 'react-dom/client'
import ImageTessellation from "./ImageTessellation.jsx";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ImageTessellation />
  </React.StrictMode>,
)
